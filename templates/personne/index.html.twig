{% extends 'base.html.twig' %}

{% block title %}Hello PersonneController!{% endblock %}

{% block body %}
<h1>Créer une personne</h1>
    {{ form_start(form) }}
        {{ form_row(form.prenom) }}
        {{ form_row(form.nom) }}
    <button class="btn btn_primary" type="submit">Valider</button>
    {{ form_end(form) }}
{% endblock %}
